{"nl": "Show top 10 sellers by number of orders", "gold_sql": "SELECT seller_id, COUNT(order_id) AS total_orders FROM order_items GROUP BY seller_id ORDER BY total_orders DESC LIMIT 10;"}
{"nl": "Show total revenue per seller", "gold_sql": "SELECT seller_id, SUM(price) AS total_revenue FROM order_items GROUP BY seller_id;"}
{"nl": "Show top 10 products by number of reviews", "gold_sql": "SELECT oi.product_id, COUNT(orv.review_id) AS review_count FROM order_items oi JOIN order_reviews orv ON oi.order_id = orv.order_id GROUP BY oi.product_id ORDER BY review_count DESC LIMIT 10;"}
{"nl": "Show average review score per seller", "gold_sql": "SELECT oi.seller_id, AVG(orv.review_score) AS avg_score FROM order_items oi JOIN order_reviews orv ON oi.order_id = orv.order_id GROUP BY oi.seller_id;"}
{"nl": "Show top 5 states by number of customers", "gold_sql": "SELECT customer_state, COUNT(customer_id) AS total_customers FROM customers GROUP BY customer_state ORDER BY total_customers DESC LIMIT 5;"}
{"nl": "Show number of products per category", "gold_sql": "SELECT product_category_name, COUNT(product_id) AS total_products FROM products GROUP BY product_category_name;"}
{"nl": "Show top 5 product categories by total revenue", "gold_sql": "SELECT p.product_category_name, SUM(oi.price) AS total_revenue FROM products p JOIN order_items oi ON p.product_id = oi.product_id GROUP BY p.product_category_name ORDER BY total_revenue DESC LIMIT 5;"}
{"nl": "Show total number of orders per month in 2024", "gold_sql": "SELECT strftime('%Y-%m', order_purchase_timestamp) AS month, COUNT(order_id) AS total_orders FROM orders WHERE strftime('%Y', order_purchase_timestamp) = '2024' GROUP BY month ORDER BY month;"}
{"nl": "Show top 5 customers with highest total payments", "gold_sql": "SELECT o.customer_id, SUM(op.payment_value) AS total_payment FROM orders o JOIN order_payments op ON o.order_id = op.order_id GROUP BY o.customer_id ORDER BY total_payment DESC LIMIT 5;"}
{"nl": "Show average number of items per order", "gold_sql": "SELECT AVG(items_count) AS avg_items_per_order FROM (SELECT order_id, COUNT(*) AS items_count FROM order_items GROUP BY order_id);"}
{"nl": "Show total freight value per seller", "gold_sql": "SELECT seller_id, SUM(freight_value) AS total_freight FROM order_items GROUP BY seller_id;"}
{"nl": "Show customers with more than 5 orders", "gold_sql": "SELECT customer_id, COUNT(order_id) AS total_orders FROM orders GROUP BY customer_id HAVING total_orders > 5;"}
{"nl": "Show top 5 products by average review score", "gold_sql": "SELECT oi.product_id, AVG(orv.review_score) AS avg_score FROM order_items oi JOIN order_reviews orv ON oi.order_id = orv.order_id GROUP BY oi.product_id ORDER BY avg_score DESC LIMIT 5;"}
{"nl": "Show number of orders with payment type 'credit_card'", "gold_sql": "SELECT COUNT(DISTINCT order_id) AS total_orders FROM order_payments WHERE payment_type = 'credit_card';"}
{"nl": "Show top 5 cities with most sellers", "gold_sql": "SELECT seller_city, COUNT(seller_id) AS total_sellers FROM sellers GROUP BY seller_city ORDER BY total_sellers DESC LIMIT 5;"}
{"nl": "Show average declared monthly revenue per business type", "gold_sql": "SELECT business_type, AVG(declared_monthly_revenue) AS avg_revenue FROM leads_closed GROUP BY business_type;"}
{"nl": "Show number of leads qualified per origin", "gold_sql": "SELECT origin, COUNT(mql_id) AS total_leads FROM leads_qualified GROUP BY origin;"}
{"nl": "Show top 5 landing pages by number of leads", "gold_sql": "SELECT landing_page_id, COUNT(mql_id) AS total_leads FROM leads_qualified GROUP BY landing_page_id ORDER BY total_leads DESC LIMIT 5;"}
{"nl": "Show average declared product catalog size per business segment", "gold_sql": "SELECT business_segment, AVG(declared_product_catalog_size) AS avg_catalog_size FROM leads_closed GROUP BY business_segment;"}
{"nl": "Show top 5 products by total quantity sold", "gold_sql": "SELECT product_id, COUNT(order_item_id) AS total_sold FROM order_items GROUP BY product_id ORDER BY total_sold DESC LIMIT 5;"}
{"nl": "Show top 5 customers with highest number of 5-star reviews", "gold_sql": "SELECT o.customer_id, COUNT(orv.review_id) AS five_star_reviews FROM orders o JOIN order_reviews orv ON o.order_id = orv.order_id WHERE orv.review_score = 5 GROUP BY o.customer_id ORDER BY five_star_reviews DESC LIMIT 5;"}
{"nl": "Show average shipping limit per seller", "gold_sql": "SELECT seller_id, AVG(julianday(shipping_limit_date) - julianday(order_purchase_timestamp)) AS avg_shipping_days FROM order_items oi JOIN orders o ON oi.order_id = o.order_id GROUP BY seller_id;"}
{"nl": "Show top 5 product categories by average price", "gold_sql": "SELECT p.product_category_name, AVG(oi.price) AS avg_price FROM products p JOIN order_items oi ON p.product_id = oi.product_id GROUP BY p.product_category_name ORDER BY avg_price DESC LIMIT 5;"}
{"nl": "Show number of orders per customer city", "gold_sql": "SELECT c.customer_city, COUNT(o.order_id) AS total_orders FROM orders o JOIN customers c ON o.customer_id = c.customer_id GROUP BY c.customer_city ORDER BY total_orders DESC;"}
{"nl": "Show top 5 states by number of products", "gold_sql": "SELECT s.seller_state, COUNT(p.product_id) AS total_products FROM sellers s JOIN order_items oi ON s.seller_id = oi.seller_id JOIN products p ON oi.product_id = p.product_id GROUP BY s.seller_state ORDER BY total_products DESC LIMIT 5;"}
{"nl": "Show top 5 products with highest freight", "gold_sql": "SELECT product_id, MAX(freight_value) AS highest_freight FROM order_items GROUP BY product_id ORDER BY highest_freight DESC LIMIT 5;"}
{"nl": "Show total orders per month for January 2024", "gold_sql": "SELECT strftime('%Y-%m-%d', order_purchase_timestamp) AS day, COUNT(order_id) AS total_orders FROM orders WHERE strftime('%Y-%m', order_purchase_timestamp) = '2024-01' GROUP BY day ORDER BY day;"}
{"nl": "Show top 5 product categories by number of reviews", "gold_sql": "SELECT p.product_category_name, COUNT(orv.review_id) AS total_reviews FROM products p JOIN order_items oi ON p.product_id = oi.product_id JOIN order_reviews orv ON oi.order_id = orv.order_id GROUP BY p.product_category_name ORDER BY total_reviews DESC LIMIT 5;"}
{"nl": "Show average declared stock per business type", "gold_sql": "SELECT business_type, AVG(average_stock) AS avg_stock FROM leads_closed GROUP BY business_type;"}
{"nl": "Show top 5 sellers by number of distinct customers served", "gold_sql": "SELECT seller_id, COUNT(DISTINCT o.customer_id) AS distinct_customers FROM order_items oi JOIN orders o ON oi.order_id = o.order_id GROUP BY seller_id ORDER BY distinct_customers DESC LIMIT 5;"}
{"nl": "Show number of customers per zip code prefix", "gold_sql": "SELECT customer_zip_code_prefix, COUNT(customer_id) AS total_customers FROM customers GROUP BY customer_zip_code_prefix ORDER BY total_customers DESC;"}
{"nl": "Show top 5 products by number of orders in 2024", "gold_sql": "SELECT product_id, COUNT(order_id) AS total_orders FROM order_items oi JOIN orders o ON oi.order_id = o.order_id WHERE strftime('%Y', o.order_purchase_timestamp) = '2024' GROUP BY product_id ORDER BY total_orders DESC LIMIT 5;"}
{"nl": "Show top 5 customers by total spent in January 2024", "gold_sql": "SELECT o.customer_id, SUM(oi.price) AS total_spent FROM orders o JOIN order_items oi ON o.order_id = oi.order_id WHERE strftime('%Y-%m', o.order_purchase_timestamp) = '2024-01' GROUP BY o.customer_id ORDER BY total_spent DESC LIMIT 5;"}
{"nl": "Show average review score per product category", "gold_sql": "SELECT p.product_category_name, AVG(orv.review_score) AS avg_score FROM products p JOIN order_items oi ON p.product_id = oi.product_id JOIN order_reviews orv ON oi.order_id = orv.order_id GROUP BY p.product_category_name;"}
{"nl": "Show total payment value per customer", "gold_sql": "SELECT o.customer_id, SUM(op.payment_value) AS total_payment FROM orders o JOIN order_payments op ON o.order_id = op.order_id GROUP BY o.customer_id;"}
{"nl": "Show top 5 products with highest average freight", "gold_sql": "SELECT product_id, AVG(freight_value) AS avg_freight FROM order_items GROUP BY product_id ORDER BY avg_freight DESC LIMIT 5;"}
{"nl": "Show number of orders per order status in 2024", "gold_sql": "SELECT order_status, COUNT(order_id) AS total_orders FROM orders WHERE strftime('%Y', order_purchase_timestamp) = '2024' GROUP BY order_status;"}
{"nl": "Show top 5 sellers by average product price", "gold_sql": "SELECT seller_id, AVG(oi.price) AS avg_price FROM order_items oi GROUP BY seller_id ORDER BY avg_price DESC LIMIT 5;"}
{"nl": "Show top 5 customers with most reviews containing 'good'", "gold_sql": "SELECT o.customer_id, COUNT(orv.review_id) AS keyword_reviews FROM orders o JOIN order_reviews orv ON o.order_id = orv.order_id WHERE orv.review_comment_message LIKE '%good%' GROUP BY o.customer_id ORDER BY keyword_reviews DESC LIMIT 5;"}
{"nl": "Show number of leads closed per seller", "gold_sql": "SELECT seller_id, COUNT(mql_id) AS closed_leads FROM leads_closed GROUP BY seller_id;"}
{"nl": "Show top 5 product categories by total payments", "gold_sql": "SELECT p.product_category_name, SUM(op.payment_value) AS total_payments FROM products p JOIN order_items oi ON p.product_id = oi.product_id JOIN order_payments op ON oi.order_id = op.order_id GROUP BY p.product_category_name ORDER BY total_payments DESC LIMIT 5;"}
{"nl": "Show average declared monthly revenue per seller", "gold_sql": "SELECT s.seller_id, AVG(lc.declared_monthly_revenue) AS avg_revenue FROM sellers s JOIN leads_closed lc ON s.seller_id = lc.seller_id GROUP BY s.seller_id;"}
{"nl": "Show top 5 customers with highest average review score", "gold_sql": "SELECT o.customer_id, AVG(orv.review_score) AS avg_score FROM orders o JOIN order_reviews orv ON o.order_id = orv.order_id GROUP BY o.customer_id ORDER BY avg_score DESC LIMIT 5;"}
{"nl": "Show top 5 cities by total orders", "gold_sql": "SELECT c.customer_city, COUNT(o.order_id) AS total_orders FROM orders o JOIN customers c ON o.customer_id = c.customer_id GROUP BY c.customer_city ORDER BY total_orders DESC LIMIT 5;"}
{"nl": "Show average product weight per seller", "gold_sql": "SELECT oi.seller_id, AVG(p.product_weight_g) AS avg_weight FROM order_items oi JOIN products p ON oi.product_id = p.product_id GROUP BY oi.seller_id;"}
{"nl": "Show top 5 products with most orders shipped over 7 days", "gold_sql": "SELECT oi.product_id, COUNT(*) AS delayed_orders FROM order_items oi JOIN orders o ON oi.order_id = o.order_id WHERE julianday(o.order_delivered_customer_date) - julianday(o.order_purchase_timestamp) > 7 GROUP BY oi.product_id ORDER BY delayed_orders DESC LIMIT 5;"}
{"nl": "Show top 5 business segments by total declared monthly revenue", "gold_sql": "SELECT business_segment, SUM(declared_monthly_revenue) AS total_revenue FROM leads_closed GROUP BY business_segment ORDER BY total_revenue DESC LIMIT 5;"}
{"nl": "Show number of orders with estimated delivery before actual delivery", "gold_sql": "SELECT COUNT(order_id) AS early_deliveries FROM orders WHERE julianday(order_estimated_delivery_date) < julianday(order_delivered_customer_date);"}
{"nl": "Show total number of products sold per seller", "gold_sql": "SELECT seller_id, COUNT(order_item_id) AS total_products_sold FROM order_items GROUP BY seller_id;"}
{"nl": "Show total revenue per month in 2024", "gold_sql": "SELECT strftime('%Y-%m', order_purchase_timestamp) AS month, SUM(price) AS total_revenue FROM order_items oi JOIN orders o ON oi.order_id = o.order_id WHERE strftime('%Y', o.order_purchase_timestamp) = '2024' GROUP BY month ORDER BY month;"}
{"nl": "Show top 5 products with most 5-star reviews", "gold_sql": "SELECT oi.product_id, COUNT(orv.review_id) AS five_star_reviews FROM order_items oi JOIN order_reviews orv ON oi.order_id = orv.order_id WHERE orv.review_score = 5 GROUP BY oi.product_id ORDER BY five_star_reviews DESC LIMIT 5;"}
{"nl": "Show total freight value per month in 2024", "gold_sql": "SELECT strftime('%Y-%m', o.order_purchase_timestamp) AS month, SUM(freight_value) AS total_freight FROM order_items oi JOIN orders o ON oi.order_id = o.order_id WHERE strftime('%Y', o.order_purchase_timestamp) = '2024' GROUP BY month ORDER BY month;"}
{"nl": "Show average payment installments per payment type", "gold_sql": "SELECT payment_type, AVG(payment_installments) AS avg_installments FROM order_payments GROUP BY payment_type;"}
{"nl": "Show number of orders delivered late", "gold_sql": "SELECT COUNT(order_id) AS late_deliveries FROM orders WHERE julianday(order_delivered_customer_date) > julianday(order_estimated_delivery_date);"}
{"nl": "Show top 5 products by total revenue", "gold_sql": "SELECT oi.product_id, SUM(oi.price) AS total_revenue FROM order_items oi GROUP BY oi.product_id ORDER BY total_revenue DESC LIMIT 5;"}
{"nl": "Show average review score per customer", "gold_sql": "SELECT o.customer_id, AVG(orv.review_score) AS avg_score FROM orders o JOIN order_reviews orv ON o.order_id = orv.order_id GROUP BY o.customer_id;"}
{"nl": "Show top 5 sellers by total freight collected", "gold_sql": "SELECT seller_id, SUM(freight_value) AS total_freight FROM order_items GROUP BY seller_id ORDER BY total_freight DESC LIMIT 5;"}
{"nl": "Show top 5 product categories with highest average product weight", "gold_sql": "SELECT p.product_category_name, AVG(p.product_weight_g) AS avg_weight FROM products p GROUP BY p.product_category_name ORDER BY avg_weight DESC LIMIT 5;"}
{"nl": "Show number of orders per seller in 2024", "gold_sql": "SELECT seller_id, COUNT(oi.order_id) AS total_orders FROM order_items oi JOIN orders o ON oi.order_id = o.order_id WHERE strftime('%Y', o.order_purchase_timestamp) = '2024' GROUP BY seller_id;"}
{"nl": "Show top 5 customers with highest number of orders in 2024", "gold_sql": "SELECT o.customer_id, COUNT(o.order_id) AS total_orders FROM orders o WHERE strftime('%Y', o.order_purchase_timestamp) = '2024' GROUP BY o.customer_id ORDER BY total_orders DESC LIMIT 5;"}
{"nl": "Show average product dimensions per product category", "gold_sql": "SELECT product_category_name, AVG(product_length_cm) AS avg_length, AVG(product_width_cm) AS avg_width, AVG(product_height_cm) AS avg_height FROM products GROUP BY product_category_name;"}
{"nl": "Show total revenue per product category", "gold_sql": "SELECT p.product_category_name, SUM(oi.price) AS total_revenue FROM products p JOIN order_items oi ON p.product_id = oi.product_id GROUP BY p.product_category_name;"}
{"nl": "Show top 5 states with most customers in 2024", "gold_sql": "SELECT customer_state, COUNT(customer_id) AS total_customers FROM customers c JOIN orders o ON c.customer_id = o.customer_id WHERE strftime('%Y', o.order_purchase_timestamp) = '2024' GROUP BY customer_state ORDER BY total_customers DESC LIMIT 5;"}
{"nl": "Show total orders with payment type 'boleto'", "gold_sql": "SELECT COUNT(DISTINCT order_id) AS total_orders FROM order_payments WHERE payment_type = 'boleto';"}
{"nl": "Show top 5 products by average price", "gold_sql": "SELECT oi.product_id, AVG(oi.price) AS avg_price FROM order_items oi GROUP BY oi.product_id ORDER BY avg_price DESC LIMIT 5;"}
{"nl": "Show number of reviews per month in 2024", "gold_sql": "SELECT strftime('%Y-%m', review_creation_date) AS month, COUNT(review_id) AS total_reviews FROM order_reviews WHERE strftime('%Y', review_creation_date) = '2024' GROUP BY month ORDER BY month;"}
{"nl": "Show top 5 sellers by total number of products sold", "gold_sql": "SELECT seller_id, COUNT(order_item_id) AS total_sold FROM order_items GROUP BY seller_id ORDER BY total_sold DESC LIMIT 5;"}
{"nl": "Show average freight per product category", "gold_sql": "SELECT p.product_category_name, AVG(oi.freight_value) AS avg_freight FROM products p JOIN order_items oi ON p.product_id = oi.product_id GROUP BY p.product_category_name;"}
{"nl": "Show top 5 customers with highest total freight spent", "gold_sql": "SELECT o.customer_id, SUM(oi.freight_value) AS total_freight FROM orders o JOIN order_items oi ON o.order_id = oi.order_id GROUP BY o.customer_id ORDER BY total_freight DESC LIMIT 5;"}
{"nl": "Show number of orders per product category", "gold_sql": "SELECT p.product_category_name, COUNT(oi.order_id) AS total_orders FROM products p JOIN order_items oi ON p.product_id = oi.product_id GROUP BY p.product_category_name;"}
{"nl": "Show top 5 products with highest number of images", "gold_sql": "SELECT product_id, product_photos_qty FROM products ORDER BY product_photos_qty DESC LIMIT 5;"}
{"nl": "Show top 5 sellers by average shipping time", "gold_sql": "SELECT seller_id, AVG(julianday(o.order_delivered_customer_date) - julianday(o.order_purchase_timestamp)) AS avg_shipping_days FROM order_items oi JOIN orders o ON oi.order_id = o.order_id GROUP BY seller_id ORDER BY avg_shipping_days ASC LIMIT 5;"}
{"nl": "Show top 5 cities by number of orders delivered late", "gold_sql": "SELECT c.customer_city, COUNT(o.order_id) AS late_orders FROM orders o JOIN customers c ON o.customer_id = c.customer_id WHERE julianday(o.order_delivered_customer_date) > julianday(o.order_estimated_delivery_date) GROUP BY c.customer_city ORDER BY late_orders DESC LIMIT 5;"}
{"nl": "Show average declared product catalog size per seller", "gold_sql": "SELECT seller_id, AVG(declared_product_catalog_size) AS avg_catalog_size FROM leads_closed GROUP BY seller_id;"}
{"nl": "Show top 5 product categories by total declared monthly revenue", "gold_sql": "SELECT p.product_category_name, SUM(lc.declared_monthly_revenue) AS total_revenue FROM products p JOIN order_items oi ON p.product_id = oi.product_id JOIN leads_closed lc ON oi.seller_id = lc.seller_id GROUP BY p.product_category_name ORDER BY total_revenue DESC LIMIT 5;"}
{"nl": "Show top 5 products with highest weight-to-price ratio", "gold_sql": "SELECT p.product_id, (p.product_weight_g / AVG(oi.price)) AS weight_price_ratio FROM products p JOIN order_items oi ON p.product_id = oi.product_id GROUP BY p.product_id ORDER BY weight_price_ratio DESC LIMIT 5;"}
{"nl": "Show total revenue per seller state", "gold_sql": "SELECT s.seller_state, SUM(oi.price) AS total_revenue FROM sellers s JOIN order_items oi ON s.seller_id = oi.seller_id GROUP BY s.seller_state;"}
{"nl": "Show top 5 sellers with most distinct products sold", "gold_sql": "SELECT seller_id, COUNT(DISTINCT product_id) AS distinct_products FROM order_items GROUP BY seller_id ORDER BY distinct_products DESC LIMIT 5;"}
{"nl": "Show number of orders delivered before estimated date", "gold_sql": "SELECT COUNT(order_id) AS early_orders FROM orders WHERE julianday(order_delivered_customer_date) < julianday(order_estimated_delivery_date);"}
{"nl": "Show top 5 products with most reviews containing 'excellent'", "gold_sql": "SELECT oi.product_id, COUNT(orv.review_id) AS excellent_reviews FROM order_items oi JOIN order_reviews orv ON oi.order_id = orv.order_id WHERE orv.review_comment_message LIKE '%excellent%' GROUP BY oi.product_id ORDER BY excellent_reviews DESC LIMIT 5;"}
{"nl": "Show top 5 customers by average payment per order", "gold_sql": "SELECT o.customer_id, AVG(op.payment_value) AS avg_payment FROM orders o JOIN order_payments op ON o.order_id = op.order_id GROUP BY o.customer_id ORDER BY avg_payment DESC LIMIT 5;"}
{"nl": "Show top 5 products with highest total freight", "gold_sql": "SELECT product_id, SUM(freight_value) AS total_freight FROM order_items GROUP BY product_id ORDER BY total_freight DESC LIMIT 5;"}
{"nl": "Show average declared monthly revenue per business type", "gold_sql": "SELECT business_type, AVG(declared_monthly_revenue) AS avg_revenue FROM leads_closed GROUP BY business_type;"}
{"nl": "Show top 5 customers with most orders over $500", "gold_sql": "SELECT o.customer_id, COUNT(oi.order_id) AS high_value_orders FROM orders o JOIN order_items oi ON o.order_id = oi.order_id WHERE oi.price > 500 GROUP BY o.customer_id ORDER BY high_value_orders DESC LIMIT 5;"}
{"nl": "Show total number of orders per seller city", "gold_sql": "SELECT s.seller_city, COUNT(oi.order_id) AS total_orders FROM sellers s JOIN order_items oi ON s.seller_id = oi.seller_id GROUP BY s.seller_city;"}
{"nl": "Show top 5 product categories by average declared product catalog size", "gold_sql": "SELECT p.product_category_name, AVG(lc.declared_product_catalog_size) AS avg_catalog_size FROM products p JOIN order_items oi ON p.product_id = oi.product_id JOIN leads_closed lc ON oi.seller_id = lc.seller_id GROUP BY p.product_category_name ORDER BY avg_catalog_size DESC LIMIT 5;"}
{"nl": "Show number of 5-star reviews per seller", "gold_sql": "SELECT oi.seller_id, COUNT(orv.review_id) AS five_star_reviews FROM order_items oi JOIN order_reviews orv ON oi.order_id = orv.order_id WHERE orv.review_score = 5 GROUP BY oi.seller_id;"}
{"nl": "Show top 5 products with highest review count per month in 2024", "gold_sql": "SELECT oi.product_id, COUNT(orv.review_id) AS monthly_reviews FROM order_items oi JOIN order_reviews orv ON oi.order_id = orv.order_id WHERE strftime('%Y', orv.review_creation_date) = '2024' GROUP BY oi.product_id ORDER BY monthly_reviews DESC LIMIT 5;"}
{"nl": "Show average declared monthly revenue per seller state", "gold_sql": "SELECT s.seller_state, AVG(lc.declared_monthly_revenue) AS avg_revenue FROM sellers s JOIN leads_closed lc ON s.seller_id = lc.seller_id GROUP BY s.seller_state;"}
{"nl": "Show total number of products per seller", "gold_sql": "SELECT seller_id, COUNT(DISTINCT product_id) AS total_products FROM order_items GROUP BY seller_id;"}
{"nl": "Show top 5 customers with most orders containing 'laptop'", "gold_sql": "SELECT o.customer_id, COUNT(oi.order_id) AS laptop_orders FROM orders o JOIN order_items oi ON o.order_id = oi.order_id JOIN products p ON oi.product_id = p.product_id WHERE p.product_category_name LIKE '%laptop%' GROUP BY o.customer_id ORDER BY laptop_orders DESC LIMIT 5;"}
{"nl": "Show number of orders per product weight range", "gold_sql": "SELECT CASE WHEN product_weight_g < 500 THEN 'Light' WHEN product_weight_g BETWEEN 500 AND 2000 THEN 'Medium' ELSE 'Heavy' END AS weight_category, COUNT(oi.order_id) AS total_orders FROM order_items oi JOIN products p ON oi.product_id = p.product_id GROUP BY weight_category;"}
{"nl": "Show top 5 products with highest number of unique customers", "gold_sql": "SELECT oi.product_id, COUNT(DISTINCT o.customer_id) AS unique_customers FROM order_items oi JOIN orders o ON oi.order_id = o.order_id GROUP BY oi.product_id ORDER BY unique_customers DESC LIMIT 5;"}
{"nl": "Show average number of products per customer", "gold_sql": "SELECT AVG(total_products) AS avg_products_per_customer FROM (SELECT customer_id, COUNT(product_id) AS total_products FROM orders o JOIN order_items oi ON o.order_id = oi.order_id GROUP BY customer_id);"}
{"nl": "Show top 5 sellers with highest total revenue per city", "gold_sql": "SELECT s.seller_city, s.seller_id, SUM(oi.price) AS total_revenue FROM sellers s JOIN order_items oi ON s.seller_id = oi.seller_id GROUP BY s.seller_city, s.seller_id ORDER BY total_revenue DESC LIMIT 5;"}
{"nl": "Show number of orders with payment type 'debit_card'", "gold_sql": "SELECT COUNT(DISTINCT order_id) AS total_orders FROM order_payments WHERE payment_type = 'debit_card';"}
{"nl": "Show top 5 products with highest average declared product catalog size per seller", "gold_sql": "SELECT oi.product_id, AVG(lc.declared_product_catalog_size) AS avg_catalog_size FROM order_items oi JOIN leads_closed lc ON oi.seller_id = lc.seller_id GROUP BY oi.product_id ORDER BY avg_catalog_size DESC LIMIT 5;"}
{"nl": "Show total number of orders per product dimension category", "gold_sql": "SELECT CASE WHEN product_length_cm < 10 THEN 'Small' WHEN product_length_cm BETWEEN 10 AND 50 THEN 'Medium' ELSE 'Large' END AS length_category, COUNT(oi.order_id) AS total_orders FROM order_items oi JOIN products p ON oi.product_id = p.product_id GROUP BY length_category;"}
{"nl": "Show top 5 customers by number of orders containing products from multiple categories", "gold_sql": "SELECT o.customer_id, COUNT(DISTINCT p.product_category_name) AS category_count FROM orders o JOIN order_items oi ON o.order_id = oi.order_id JOIN products p ON oi.product_id = p.product_id GROUP BY o.customer_id HAVING category_count > 1 ORDER BY category_count DESC LIMIT 5;"}
{"nl": "Show average order value per customer city", "gold_sql": "SELECT c.customer_city, AVG(oi.price) AS avg_order_value FROM orders o JOIN customers c ON o.customer_id = c.customer_id JOIN order_items oi ON o.order_id = oi.order_id GROUP BY c.customer_city;"}